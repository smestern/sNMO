Brian2>=2.5.4
elephant>=1.0.0
joblib==1.2.0
matplotlib==3.7.2
neo==0.13.0 
networkx==3.1
nevergrad==1.0.0
pandas==2.0.3
POT>=0.9.3
pyabf
sbi==0.21.0
scikit_optimize==0.9.0
scipy>=1.11.1
torch==2.1.0
pynwb
#ideally we would like to included ipfx here but it has some errors installing in a fresh environment
# so run the following command to install it manually
# pip install ipfx --no-deps
